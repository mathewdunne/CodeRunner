/**
 * Two tabs of the same workspace stay in DS sync (decision 015).
 */
import { test } from "@playwright/test";

test("two tabs reflect Enable from either tab", async () => {
  test.fixme(true, "Needs DS data-testids on enable button and status pill.");
});
